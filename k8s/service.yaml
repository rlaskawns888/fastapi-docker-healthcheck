# kubectl apply -f k8s/service.yaml
# kubectl port-forward service/my-nginx 8080:80

apiVersion: v1
kind: Service
metadata:
  name: my-nginx
spec:
  selector:
    app: my-nginx
  ports:
    - port: 80
      targetPort: 80